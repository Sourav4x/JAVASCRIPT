const h="sourav"  // string declaration method
let c=new String("hello it's me ") // another method of string declaration 
// all string methods are available in this declaration
let v=c.substr(0,4)
let q=c.indexOf("l")
console.log(q)
console.log(c.replace("l","m"))
console.log(c.trim())
console.log(v)
console.log(c.includes("me"))
console.log(c.split(" "))  // converted into array using delimeter " "
